name: Backend Tests

on:
  push:
    branches:
      - main
      - master
  pull_request:
    branches:
      - main
      - master

jobs:
  build:
    name: Build Project
    runs-on: ubuntu-latest
    container:
      image: maven:3.8.6-openjdk-18
      options: --mount type=volume,source=maven-repo,target=/root/.m2 --mount type=volume,source=maven-build,target=/github/workspace/target
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Build project
        run: mvn clean install -DskipTests

  unit-test:
    name: Run Unit Tests
    runs-on: ubuntu-latest
    container:
      image: maven:3.8.6-openjdk-18
      options: --mount type=volume,source=maven-repo,target=/root/.m2 --mount type=volume,source=maven-build,target=/github/workspace/target
    needs: build
    steps:
      - name: Run unit tests
        run: mvn test -Psmall-test

  integration-test:
    name: Run Integration Tests
    runs-on: ubuntu-latest
    container:
      image: maven:3.8.6-openjdk-18
      options: --mount type=volume,source=maven-repo,target=/root/.m2 --mount type=volume,source=maven-build,target=/github/workspace/target
    needs: build
    steps:
      - name: Run integration tests
        run: mvn test -Pmedium-test
