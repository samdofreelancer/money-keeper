apiVersion: v1
kind: Service
metadata:
  name: money-keeper-backend-service
  namespace: money-keeper
  labels:
    app: money-keeper-backend
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: money-keeper-backend
