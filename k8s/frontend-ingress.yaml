apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: money-keeper
  namespace: money-keeper
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
spec:
  ingressClassName: traefik
  rules:
  - http:
      paths:
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: money-keeper-backend
            port:
              number: 8080
      - path: /
        pathType: Prefix
        backend:
          service:
            name: money-keeper-frontend-service
            port:
              number: 80
