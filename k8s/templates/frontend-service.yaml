apiVersion: v1
kind: Service
metadata:
  name: money-keeper-frontend-service
spec:
  selector:
    app: money-keeper-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP