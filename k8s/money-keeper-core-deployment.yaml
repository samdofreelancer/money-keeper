apiVersion: apps/v1
kind: Deployment
metadata:
  name: money-keeper-core
  namespace: money-keeper
  labels:
    app: money-keeper-core
spec:
  replicas: 1
  selector:
    matchLabels:
      app: money-keeper-core
  template:
    metadata:
      labels:
        app: money-keeper-core
    spec:
      serviceAccountName: default
      containers:
      - name: money-keeper-core
        image: ghcr.io/samdofreelancer/money-keeper-core:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8081
          name: http
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: "ci"
        - name: SPRING_DATASOURCE_URL
          value: "jdbc:oracle:thin:@oracle:1521/XEPDB1"
        - name: SPRING_DATASOURCE_USERNAME
          value: "system"
        - name: SPRING_DATASOURCE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: oracle-secret
              key: password
        - name: ORACLE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: oracle-secret
              key: password
        - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
          value: "http://eureka-server-service:8761/eureka/"
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8081
          initialDelaySeconds: 60
          periodSeconds: 30
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8081
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: money-keeper-core-service
  namespace: money-keeper
spec:
  selector:
    app: money-keeper-core
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081
      name: http
  type: ClusterIP
